services:
  db:
    image: postgres:17
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: simmer
      POSTGRES_PASSWORD: simmer
      POSTGRES_DB: simmer
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
